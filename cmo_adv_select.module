<?php

/**
 * @file
 * Advanced Webform Select Option
 */

function cmo_adv_select_form_alter(&$form, &$form_state, $form_id){
  
  if(isset($form['node'])){
    dsm($form);
    dsm($form_state);
  }
}